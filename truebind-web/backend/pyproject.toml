[project]
name = "truebind-backend"
version = "0.1.0"
description = "Truebind FastAPI backend (Next.js + FastAPI redesign, Phase 0)"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
